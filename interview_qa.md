# 面试问答私库 · Aivalon

> 本文件已 gitignore，不入库。沉淀每个功能模块的"面试可能怎么考"+ 答题骨架。
> 规则：问题来自真实开发过程（见 DEVLOG），答案要有实战细节，不写八股空话。
> 概念细节查 `document/DEVLOG.md` 的「概念速查」栏目（C01/C02/...）。

## 可观测性（A 组）

### Q1 你们的监控体系是怎么搭的？
**骨架**：三支柱分工——metrics 看趋势（Prometheus + Grafana）、日志查个案（trace_id 串联）、trace 看调用链。我们项目里：FastAPI 用 instrumentator 自动采集 HTTP 指标，业务指标（WS 连接数、活跃房间、AI 队列积压）用 prometheus-client 自定义，Grafana 全声明式预置一条命令起。
**加分细节**：label 基数控制（见 Q3）；Histogram 桶按目标延迟设计（见 Q4）；踩过依赖版本冲突的坑（DEVLOG 002）。

### Q2 Prometheus 拉模型（pull）是什么？有什么优缺点？还有什么方案？
**是什么**：Prometheus 定期（我们配 5s）主动 HTTP 抓取各目标的 `/metrics` 端点，而不是等应用上报。
**优点**：①被监控方零侵入，暴露个端点就行；②目标宕机能立刻发现（抓取失败 = targets 页 DOWN）；③采样节奏由 Prometheus 控制，天然防背压；④配置集中（一个 prometheus.yml 管所有目标）。
**缺点**：①要知道目标在哪——大规模下靠服务发现（consul/k8s SD）而不是手写 IP；②短命任务（几秒就跑完的批处理）可能在两次抓取间隔内消失，抓不到；③跨网络麻烦（NAT/防火墙另一侧的目标拉不到——我们容器拉宿主机就用了 host.docker.internal）。
**平行方案**：
- **Pushgateway**（Prometheus 官方补充）：短命任务把指标推给它，Prometheus 再拉它。坑：它会成为单点，且任务死了指标还留着，需要显式清理
- **纯推模型**：StatsD（UDP 一发即走，不保证送达）、InfluxDB（应用主动写）、Datadog（agent 推送 SaaS）——适合边缘设备、客户端上报这种"服务端够不着我"的场景
- **OpenTelemetry**：CNCF 的统一标准，指标/日志/链路三合一，推拉都支持，厂商中立——行业演进方向，面试提它显见识

### Q3 什么是 label 基数爆炸？
**骨架**：label 每种取值组合 = 一条独立时间序列，都占内存。放路由/状态码这种低基数维度没问题（几十条序列）；放 game_id/user_id 这种高基数值，一万房间 = 上万条序列，Prometheus 内存打爆。正确做法：高基数维度走日志（trace_id），metrics 只做聚合趋势。
**关联**：DEVLOG C02。

### Q4 你的 P99 是怎么算的？准吗？
**骨架**：Histogram 不存原始值，按桶累计计数；P99 是找到第 99% 位置落在哪个桶，桶内假设均匀分布做线性插值——是估算值。精度取决于桶边界设计：我们目标验证 P99 < 100ms，所以 5ms~200ms 区间桶设得最密（`metrics.py` buckets 参数）。
**追问预案**："要精确值怎么办？"——精确分位数要原始数据，可用日志侧统计或 t-digest 类结构，但成本高；工程上桶设计合理的估算完全够用。
**关联**：DEVLOG C03。

### Q5 容器里的服务怎么访问宿主机？（容器网络）
**骨架**：容器内 localhost 是容器自己；macOS/Windows Docker Desktop 提供 `host.docker.internal` 特殊域名指向宿主机；Linux 需要 `extra_hosts: host-gateway` 显式映射（我们 compose 里两个都写了）。
**延伸**：同 compose 网络内服务互访用服务名（Grafana 配数据源就是 `http://prometheus:9090`）。

### Q6 功能没动过但服务起不来，怎么排查？
**骨架**：先怀疑环境/版本，再怀疑代码。真实案例：装新依赖后 uvicorn 崩，报错栈指向业务代码（受害者），`pip check` 一把定位到真凶——instrumentator 8.x 强升 starlette 拖垮 fastapi。修复后在 requirements 留版本上限 + 注释防复发。
**关联**：DEVLOG 002。

### Q7 怎么定位"某一次具体请求"为什么慢？（全链路追踪）
**骨架**：metrics 只能告诉你"P99 变差了"，定位个案靠 trace_id——每个请求入口生成或接续 X-Request-ID，存进 contextvars（协程安全，并发请求互不串扰），日志过滤器给每条日志自动注入；响应头带回 ID，压测流量可以自带标记。拿到 ID 后 grep 日志即得该请求完整轨迹。
**追问预案**："跨服务/跨进程怎么办？"——异步入队时把 trace_id 写进消息体（我们的 Outbox payload / Celery task 参数），消费端接续同一 ID；成熟方案是 OpenTelemetry 的 traceparent 标准头。
**关联**：`backend/app/core/tracing.py`。

## 压测（B 组）

### Q8 压测方案怎么设计的？为什么登录接口不压？
**骨架**：Locust（Python 同栈，可复用协议结构）+ 五类标准场景（纯 API / 对局主链路 / 长连接 / 突发流量 / 热点场景）。登录接口有 5次/分/IP 限流，压它测的是限流器而不是系统容量——压测对象要想清楚；压测 token 由数据脚本直接签发旁路登录。
**加分细节**：压测流量带 `bench-*` trace_id 前缀可与真实流量隔离；注册验证码走 Redis 旁路跳过邮件，但业务校验逻辑不变；测试邮箱不能用 `.local` 等特殊用途 TLD（email-validator 会拒，见 DEVLOG 003）。
**追问预案**："Locust 单机压不动怎么办？"——Locust 支持多 worker 分布式（--master/--worker），单机 gevent 协程数千并发用户起步；S3 长连接场景如到瓶颈会引入 k6 或多机 Locust。

### Q9 压测报告的完整性应该包含什么？
**骨架**：环境口径（硬件、docker 资源限制、代码版本 commit）、参数（并发梯度、爬升速率、持续时间）、结果（QPS/分位数/错误率，附 Grafana 截图）、瓶颈定位（第一瓶颈是什么、证据是什么）、结论与下一步。缺了环境口径的数字不可复现，等于没测。

### Q10 限流按什么维度？IP 还是用户？
**骨架**：分场景——未认证入口（登录/注册）按 IP 防爆破；认证接口按 user_id，因为按 IP 有两个致命伤：NAT/公司出口多用户共享配额被误伤、代理池换 IP 轻松绕过。我们项目的真实案例：v1 用 fastapi-limiter 默认按 IP，压测时单 IP 全局 1 动作/秒直接跑不了，重构为 JWT 解析 user_id + 阈值可配置（DEVLOG 004）。
**追问预案**："分布式下限流计数存哪？"——Redis 集中计数（我们就是）；量级再大可做本地计数 + 周期同步的近似限流，牺牲精度换性能。


## 房间热路径（D 组）
（待 D 组完成时补充）

## 长连接与广播（E 组）

### Q11 长连接服务的容量上限由什么决定？怎么排查？
**骨架**：每连接持有成本审计——FD、内存缓冲区、线程、DB 连接，任何一项被独占都是上限。真实案例：我们的 WS 鉴权用 FastAPI yield 依赖注入 DB Session，连接存续期间独占池连接，默认池 15 个 → 第 16 条 WS 连接开始排队 → 线程池堵死 → accept 积压 → 整个服务（含健康检查）雪崩。修复：握手鉴权改短生命周期 Session。修复前 100 连接 93% 失败，修复后 1000 连接 0 失败（DEVLOG 006）。
**追问预案**："怎么定位的？"——事件循环空闲 + TCP 超时 = accept 积压，逆推线程池 → 连接池；日志里 QueuePool TimeoutError 是实锤。排除法先排除新引入的中间件（A/B 复测）。

## 缓存与热榜（F/G 组）
（待 F/G 组完成时补充）

## 缓存与热榜（F/G 组）
（待 F/G 组完成时补充）

## 韧性工程（H 组）
（待 H 组完成时补充）
