# 面试问答私库 · Aivalon

> 沉淀每个功能模块的"面试可能怎么考"+ 答题骨架（本文件随代码入库，commit 1b08cb8 起）。
> 规则：问题来自真实开发过程（见 DEVLOG），答案要有实战细节，不写八股空话。
> 概念细节查 `document/DEVLOG.md` 的「概念速查」栏目（C01/C02/...）。

## 可观测性（A 组）

### Q1 你们的监控体系是怎么搭的？
**骨架**：三支柱分工——metrics 看趋势（Prometheus + Grafana）、日志查个案（trace_id 串联）、trace 看调用链。我们项目里：FastAPI 用 instrumentator 自动采集 HTTP 指标，业务指标（WS 连接数、活跃房间、AI 队列积压）用 prometheus-client 自定义，Grafana 全声明式预置一条命令起。
**加分细节**：label 基数控制（见 Q3）；Histogram 桶按目标延迟设计（见 Q4）；踩过依赖版本冲突的坑（DEVLOG 002）。

### Q2 Prometheus 拉模型（pull）是什么？有什么优缺点？还有什么方案？
**是什么**：Prometheus 定期（我们配 5s）主动 HTTP 抓取各目标的 `/metrics` 端点，而不是等应用上报。
**优点**：①被监控方零侵入，暴露个端点就行；②目标宕机能立刻发现（抓取失败 = targets 页 DOWN）；③采样节奏由 Prometheus 控制，天然防背压；④配置集中（一个 prometheus.yml 管所有目标）。
**缺点**：①要知道目标在哪——大规模下靠服务发现（consul/k8s SD）而不是手写 IP；②短命任务（几秒就跑完的批处理）可能在两次抓取间隔内消失，抓不到；③跨网络麻烦（NAT/防火墙另一侧的目标拉不到——我们容器拉宿主机就用了 host.docker.internal）。
**平行方案**：
- **Pushgateway**（Prometheus 官方补充）：短命任务把指标推给它，Prometheus 再拉它。坑：它会成为单点，且任务死了指标还留着，需要显式清理
- **纯推模型**：StatsD（UDP 一发即走，不保证送达）、InfluxDB（应用主动写）、Datadog（agent 推送 SaaS）——适合边缘设备、客户端上报这种"服务端够不着我"的场景
- **OpenTelemetry**：CNCF 的统一标准，指标/日志/链路三合一，推拉都支持，厂商中立——行业演进方向，面试提它显见识

### Q3 什么是 label 基数爆炸？
**骨架**：label 每种取值组合 = 一条独立时间序列，都占内存。放路由/状态码这种低基数维度没问题（几十条序列）；放 game_id/user_id 这种高基数值，一万房间 = 上万条序列，Prometheus 内存打爆。正确做法：高基数维度走日志（trace_id），metrics 只做聚合趋势。
**关联**：DEVLOG C02。

### Q4 你的 P99 是怎么算的？准吗？
**骨架**：Histogram 不存原始值，按桶累计计数；P99 是找到第 99% 位置落在哪个桶，桶内假设均匀分布做线性插值——是估算值。精度取决于桶边界设计：我们目标验证 P99 < 100ms，所以 5ms~200ms 区间桶设得最密（`metrics.py` buckets 参数）。
**追问预案**："要精确值怎么办？"——精确分位数要原始数据，可用日志侧统计或 t-digest 类结构，但成本高；工程上桶设计合理的估算完全够用。
**关联**：DEVLOG C03。

### Q5 容器里的服务怎么访问宿主机？（容器网络）
**骨架**：容器内 localhost 是容器自己；macOS/Windows Docker Desktop 提供 `host.docker.internal` 特殊域名指向宿主机；Linux 需要 `extra_hosts: host-gateway` 显式映射（我们 compose 里两个都写了）。
**延伸**：同 compose 网络内服务互访用服务名（Grafana 配数据源就是 `http://prometheus:9090`）。

### Q6 功能没动过但服务起不来，怎么排查？
**骨架**：先怀疑环境/版本，再怀疑代码。真实案例：装新依赖后 uvicorn 崩，报错栈指向业务代码（受害者），`pip check` 一把定位到真凶——instrumentator 8.x 强升 starlette 拖垮 fastapi。修复后在 requirements 留版本上限 + 注释防复发。
**关联**：DEVLOG 002。

### Q7 怎么定位"某一次具体请求"为什么慢？（全链路追踪）
**骨架**：metrics 只能告诉你"P99 变差了"，定位个案靠 trace_id——每个请求入口生成或接续 X-Request-ID，存进 contextvars（协程安全，并发请求互不串扰），日志过滤器给每条日志自动注入；响应头带回 ID，压测流量可以自带标记。拿到 ID 后 grep 日志即得该请求完整轨迹。
**追问预案**："跨服务/跨进程怎么办？"——异步入队时把 trace_id 写进消息体（我们的 Outbox payload / Celery task 参数），消费端接续同一 ID；成熟方案是 OpenTelemetry 的 traceparent 标准头。
**关联**：`backend/app/core/tracing.py`。

## 压测（B 组）

### Q8 压测方案怎么设计的？为什么登录接口不压？
**骨架**：Locust（Python 同栈，可复用协议结构）+ 五类标准场景（纯 API / 对局主链路 / 长连接 / 突发流量 / 热点场景）。登录接口有 5次/分/IP 限流，压它测的是限流器而不是系统容量——压测对象要想清楚；压测 token 由数据脚本直接签发旁路登录。
**加分细节**：压测流量带 `bench-*` trace_id 前缀可与真实流量隔离；注册验证码走 Redis 旁路跳过邮件，但业务校验逻辑不变；测试邮箱不能用 `.local` 等特殊用途 TLD（email-validator 会拒，见 DEVLOG 003）。
**追问预案**："Locust 单机压不动怎么办？"——Locust 支持多 worker 分布式（--master/--worker），单机 gevent 协程数千并发用户起步；S3 长连接场景如到瓶颈会引入 k6 或多机 Locust。

### Q9 压测报告的完整性应该包含什么？
**骨架**：环境口径（硬件、docker 资源限制、代码版本 commit）、参数（并发梯度、爬升速率、持续时间）、结果（QPS/分位数/错误率，附 Grafana 截图）、瓶颈定位（第一瓶颈是什么、证据是什么）、结论与下一步。缺了环境口径的数字不可复现，等于没测。

### Q10 限流按什么维度？IP 还是用户？
**骨架**：分场景——未认证入口（登录/注册）按 IP 防爆破；认证接口按 user_id，因为按 IP 有两个致命伤：NAT/公司出口多用户共享配额被误伤、代理池换 IP 轻松绕过。我们项目的真实案例：v1 用 fastapi-limiter 默认按 IP，压测时单 IP 全局 1 动作/秒直接跑不了，重构为 JWT 解析 user_id + 阈值可配置（DEVLOG 004）。
**追问预案**："分布式下限流计数存哪？"——Redis 集中计数（我们就是）；量级再大可做本地计数 + 周期同步的近似限流，牺牲精度换性能。


## 房间热路径（D 组）

### Q12 连接池满了，调大池子能解决吗？
**骨架**：先分清是**慢**还是**死锁**，不同病吃不同药。慢（过载）：持有者都在跑查询，压力降下来自愈，调大池或优化查询有效。死锁：持有者全部空闲却不放手，永不自愈——调大池只是把死亡阈值抬高，不解决任何问题。
**实战案例（我们踩的）**：创建对局路径需要**两个**连接——鉴权用 FastAPI yield 依赖拿第一个（持有到请求结束），下游查用户名再开 Session 拿第二个。20 并发下 15 个连接全被鉴权占满，然后它们**集体在等第二个连接**——占着资源的正是在等资源的人，池永不恢复，全部卡到 30s 超时，创建对局失败率 25.76%。池大小 15 在这里能撑的真实并发只有 7 个多。
**关键算式**：**有效并发 = 池大小 ÷ 单请求峰值持有连接数**。审计资源要按峰值持有量算，不是看池大小。
**追问预案**："那这个 15 调到 40 呢？"——死亡阈值从 7 并发抬到 20 并发，S2 跑 20 房间刚好又挂；而且掩盖了问题，下次流量涨一点重来一遍。
**关联**：DEVLOG 012。

### Q13 怎么判断连接池满是死锁还是过载？
**骨架**：**看持有者在干什么**。MySQL `processlist` 里持有连接的会话都在 `Query` 状态 = 真的忙不过来（过载）；全部是 `Sleep` 空闲而应用侧请求在阻塞 = 有人签出后握着不放，往死锁/泄漏方向查。
**为什么这条证据一开始没读懂**：我们看到 processlist 全 Sleep，第一反应是"连接泄漏"（签出永不归还），实际是"周期性僵局"——连接被握着，30s 超时后随请求崩掉全部归还，然后下一批再死一次。泄漏是单调累积，死锁是周期复发，两者现象像但修法不同。
**死锁四条件**（这个案例全齐）：互斥持有、**持有并等待**、不可抢占、循环等待。破解通常打第二条——别握着 A 去要 B。
**追问预案**："MySQL 侧看不出来怎么办？"——见 Q14。

### Q14 应用侧怎么定位"谁签出了连接不归还"？
**骨架**：信息在哪一侧就在哪一侧埋点。MySQL 只知道"有客户端连着不说话"，不知道是哪段代码借的——这个信息只存在连接池这一侧。做法：在池的 `checkout`/`checkin` 事件上挂监听（SQLAlchemy 原生支持），checkout 时记下**时间 + trace_id + 业务调用栈**，checkin 时删除；后台循环周期扫描"持有超过阈值仍未归还"的，打出来。
**为什么埋在池事件而不是业务代码**：取 Session 的地方有几十处，逐个埋会漏；池的借还是**唯一必经通道**，在框架收口处埋是通用做法（同理：trace_id 埋在中间件而不是每个 handler）。
**实战效果**：一轮压测直接锁定——15 个连接全部签出于 `deps.py:46`（鉴权那句 query），持有 5.9s，池水位连续 24 秒钉在 15/15；trace_id 反查访问日志，这 15 个请求全是创建对局、最终 ERR 于 30.1s；超时点分两处 12 + 5 = 17，正好等于 500 的总数，账完全对上。
**加分细节**：①探针默认关闭（`DB_POOL_PROBE` 环境变量），因为每次 checkout 抓调用栈有开销，是排障手电筒不是长明灯——常驻的那份是池水位 Gauge，便宜但只看趋势（**指标看趋势、探针查个案**，同 C02）；②每个可疑连接只报一次，否则日志被刷爆；③时间差一律用 `monotonic` 不用 `time.time()`，避免 NTP 校时算出负数。
**关联**：`backend/app/core/pool_probe.py`，DEVLOG 012。

### Q15 讲一个"修复引入新 bug"的经历？
**骨架**（这题答真实案例比编的有说服力）：为了修"连接被持有着跨 await 等待"，我把创建对局里复用同一 Session 的用户名查询抽成了独立函数 + 独立短生命周期 Session。这个改动**局部完全正确**——连接确实不再跨 await 持有了；但它把单请求峰值持有连接数从 1 抬到了 2，于是造出了上面那个自死锁，整体比改之前更糟（原来是慢，现在是死）。
**教训**：**改资源生命周期时必须重算全局资源账**，问一句"改完这条路径峰值持有几个连接"。局部最优 ≠ 全局最优，资源类改动尤其明显。
**追问预案**："那正确修法是什么？"——三条路：复用同一 Session（最简单）、缩短前一个连接的生命周期让它用完即还（我们最终选的）、或给不同用途拆独立池做舱壁（后台刷库器用的这条）。我们选第二条，因为鉴权本来就是一次性查询，没有任何理由把连接持有到请求结束。

### Q16 同一个 bug 类型在别处还有吗？（修完一处的动作）
**骨架**：修完立刻 grep 同类写法。我们这个 yield 依赖持有连接的病，**早在两个月前的 WS 长连接场景就修过一次**（握手鉴权改短生命周期 Session，100 连接 93% 失败 → 1000 连接 0 失败），但当时只改了 WS 路径，HTTP 鉴权路径同病同因却漏了——直到压测再把它撞出来。
**教训**：修复的收尾动作是"搜同类"，不是"测通过"。同一个错误模式在一个代码库里很少只出现一次。
**关联**：DEVLOG 006（WS 路径）→ DEVLOG 012（HTTP 路径），同一个病修了两次。

### Q17 为什么用 Actor 单写者模型替代 Redis 分布式锁？
**骨架**：锁防的是"并发改同一房间"，单写者模型下同一房间的动作在一个执行单元的队列里串行消费，**没有并发写，锁失去意义**。收益：①省掉每次动作的加锁/解锁网络往返；②串行化天然保证事件顺序；③检查-再行动的 TOCTOU 竞态在模型层面消除（我们压测撞到过 AI 重复投票，DEVLOG 005）。
**加分细节**：房间空闲 300s 自动退出 Actor 释放资源，状态留内存，下次动作自动唤醒重建。
**追问预案**："单点怎么办？"——需要房间到节点的路由（一致性哈希 + 路由表存 Redis）和节点宕机时的房间漂移，这是 D-3 在做的事；单写者的代价就是把并发问题换成了路由与故障转移问题。
**关联**：`backend/app/core/room_actor.py`，DEVLOG 005。

### Q18 你们的 RPO 是多少？怎么测出来的？
**骨架**：RPO 要**拆两个维度**报，只说一个数是不完整的。时间维度：刷库间隔 200ms，最坏丢 ≤200ms——这个看设计就知道。条数维度：已入 Redis Stream 但刷库游标尚未推进的事件数——**这个只能实测**，因为它取决于负载。我们的实测值：20 并发房间满载下高频采样 427 次，**峰值 16 条**（最坏 RPO）、P95 10 条、均值 3.45 条、84.5% 的时刻窗口非空。
**测法**：写个采样脚本，每 100ms 读一次"Stream 里游标之后还剩多少条"，取峰值。
**踩过的坑**：第一次空载跑，427 个采样全是 0——因为演练脚本 0.4s 一个动作，比 200ms 刷库慢，窗口永远追平。**这种测量必须在负载下做，空跑出来的漂亮 0 是假数据**。
**追问预案**："想把 RPO 降到 0 怎么办？"——那就得放弃 Write-Behind 回到同步写，或者上组复制/半同步复制把持久化点前移；RPO 和写吞吐是明摆着的取舍，我们选了"200ms/16 条"换掉了同步写 MySQL 的容量天花板。
**关联**：`bench/drill_kill_recovery.py`，DEVLOG 013。

### Q19 宕机恢复演练怎么设计？
**骨架**：**按故障域分开设计，别笼统说"宕机"**。我们 Write-Behind 架构下，"API 挂"和"Redis 挂"后果完全不同：
- API 进程挂：Redis 里状态快照和事件都完好 → **零丢失**，重启后对局直接续打
- Redis 也挂：丢的才是上面那个 RPO 窗口

笼统说"宕机恢复"会把零丢失的场景和有损场景混为一谈，面试官一追问就露馅。
**实测**：kill -9 API（**注意刷库器跑在 API 进程内，等于同时杀掉了它**）→ 重启 → 对局从 `phase=vote round=2` 读回 `phase=mission round=2`，续打到结算；最终 101 条事件、distinct seq 101、**重复行 0**（刷库器崩溃重刷靠 `INSERT IGNORE` 幂等兜住）。
**加分细节**：①演练脚本做成可重复执行的四段式（建局打牌 / 测量 / 恢复续打 / RPO 采样），一次性手敲的演练等于没做，后面节点迁移演练还要复用；②演练要**主动对账**（事件数 vs max seq、seq 连续性），这个动作本身挖出了一个静默丢数据的 bug，见 Q20；③诚实报边界——我们从 DB 重建对局状态的分支还是 TODO，所以恢复能力目前依赖 Redis 快照存在，这一点不含糊过去。
**关联**：DEVLOG 013。

### Q20 `INSERT IGNORE` 有什么坑？
**骨架**：**它忽略的错误远超你的预期**——不只是唯一键冲突，还包括外键失败、类型截断、NOT NULL 违反，全部降级成 warning，语句返回成功，事务照常提交。你想要的是其中一种，拿到的是一整类。
**实战案例（我们踩的）**：批量刷库器用 `INSERT IGNORE` 做崩溃重刷的幂等（游标提交后才推进，崩溃会重刷同一批）。但 `game_events.game_id` 有外键指向 `games.id`，而刷库循环里**先插事件行、后补建 games 记录**。一局的第一条事件 `GAME_START` 落库时 games 还没有这行 → 外键失败 → 被 IGNORE 静默吞掉。结果：**全库 372 局，GAME_START 事件 0 条**，零日志零异常，24 项测试全绿。
**怎么发现的**：宕机演练顺手对账——事件数 101 但 `max(seq)=102`，差 1；查 seq 连续性发现 `min=2` 区间无缺口，唯独 seq=1 不存在，而 seq=1 是写死的 GAME_START。
**修法**：把 games 记录的补建提到事件插入之前（同一事务内，依赖行先于依赖者）。
**更精确的替代**：`ON DUPLICATE KEY UPDATE`（只针对键冲突）或先查后插——想忽略哪种错误就只忽略那种。
**教训**：①**"写成功"和"写进去了"是两件事**；②静默失败靠对账和不变量校验（序号连续、计数一致）发现，指望报错是没有的；③集成测试断言的是"流程能走通"，不是"数据完整"，断言层级不同，抓不到这类 bug。
**关联**：DEVLOG 014。

### Q21 你说"MySQL 写压力下降 80%"，这个 80% 是什么的 80%？
**骨架**（这题的陷阱在指标定义，答不清楚就是编数字）：是**写事务数**下降 80%，不是写入量。分两个维度看：
- **行数维度：下降 0%**。Write-Behind 不减少落库行数——事件该持久化的还是要持久化，只是晚 200ms。实测 `Innodb_rows_inserted` 每事件 2.0135 → 2.0138，纹丝不动
- **事务维度：下降 78~81%**。v1 是"一个动作一个事务"，v2 是"一批事件一个事务"。实测每事件 `Com_commit` 1.07 → 0.20~0.24，等于平均 **4~5 个事件合并进一个事务**

**为什么事务数才是瓶颈**：每次 commit 要 fsync 刷盘（磁盘同步，最贵的一步）+ 占一个连接一个网络往返。省掉 80% 的 commit 就是省掉 80% 的 fsync。
**如果指标选错会怎样**：拿"写入行数"当指标，测出来是 0%，看起来像优化白做了——真实的 4~5 倍改善被错误的指标藏起来了。**优化前必须先问"我省掉的到底是什么资源"**。
**追问预案**："那省下的 80% 换来了什么代价？"——RPO 从 0 变成"最坏 16 条 / 200ms 窗口"（见 Q18），明摆着的取舍，不是免费的。
**关联**：DEVLOG 015。

### Q22 优化前后对比，你怎么保证数字是公平的？
**骨架**：四条，缺一条数字就可疑。
1. **对照组要真跑，不能引用旧报告**。我们的基线报告恰好只记了定性结论（"写路径饱和"），没有绝对写 QPS——硬凑对比就变成编数字。做法：`git worktree add` 检出优化前那个 commit，起完整旧版栈，跑同一个压测脚本
2. **只差一个变量**。对照组 commit 选的是"Actor 已做完、Write-Behind 还没做"那一个，不是更早的——否则测出来是多个改动的叠加，归因不了
3. **同机、同中间件、同压测参数**。跨机的绝对延迟不可比（我们换过机器，基线是 Intel 上测的，这一条项目里专门标注了口径）
4. **按业务量归一化**。优化让系统变快，同样时长会处理更多事件——直接比绝对 QPS 会把"变快了"和"变省了"混在一起。除以事件数（每事件 commit 次数）才是可比口径

**加分细节**：各跑两轮。我们 v1 两轮 1.0734/1.0780、v2 两轮 0.2036/0.2387——**一致性本身就是数据可信的证据**，单轮数字无法排除偶发因素（这个规矩是踩过坑来的：曾经一轮压测通过就宣布修好，结果复测又崩，见 DEVLOG 011）。
**测量手段**：`SHOW GLOBAL STATUS` 计数器在压测前后打点取增量（`Com_commit`/`Com_insert`/`Innodb_rows_inserted`/`Queries`），脚本化成 `start/stop/diff` 三段可复用。
**关联**：`bench/measure_write_pressure.py`，DEVLOG 015。

### Q23 为什么路由用一致性哈希而不是取模？
**骨架**：分母不一样。`hash(game_id) % N` 的分母是节点数，3 台扩到 4 台，分母从 3 变 4，约 **3/4** 的房间换主。环的分母是环本身（0~2^128），摘掉一个节点只影响它那段弧，期望迁移量 **1/N**。
**为什么迁移量是核心指标**：房间不是无状态请求。换主意味着状态从 Redis 快照重建 + Actor 重建，迁移量就是代价本身。取模扩容一次等于全集群重建，环只重建那一个节点的份额。
**追问预案**："虚拟节点解决什么？"——3 个物理节点把环切成 3 段随机长度的弧，倾斜可能到 60/30/10。每个物理节点放 160 个虚拟点（libketama 的经典值），靠大数定律摊平弧长，实测分布偏差约 5%。**虚拟节点名必须由节点名派生**（`node#0`~`node#159`）而不是随机生成，否则节点重启后落点全变，名下房间不会漂回来。

### Q24 一致性哈希实现里最容易踩的坑是什么？
**骨架**：不能用 Python 内置 `hash()`。CPython 对 `str` 的 `hash()` 带**每进程随机盐**（`PYTHONHASHSEED`），同一个字符串在不同进程算出不同值。
**后果不是"结果略有差异"**：每个节点算出的路由表都不一样 → **同一个房间被两个节点同时认领** → 单写者模型作废、状态分叉。这类 bug 单机永远不出现，扩容后才炸，且不报错。
**解法**：换跨进程稳定的哈希，我用 md5（取分布性，不取密码学强度）。
**怎么保证不回归**：单测里**起一个子进程**重算同一个 key 的哈希再断言相等——这是唯一能抓到盐陷阱的测法，同进程内跑一万次都是通过的。
**推广**：凡是**跨进程比较**的哈希都适用——路由、分片、去重、缓存键。
**关联**：`backend/app/core/hash_ring.py`，`tests/test_hash_ring.py::test_hash_is_stable_across_processes`，DEVLOG C05。

### Q25 节点存活怎么判断？阈值怎么定？
**骨架**：心跳租约，不主动探活。节点自己上报，"最后心跳早于 TTL"即视为死。k8s node lease、Nacos/Eureka 续约、Kafka session timeout 都是这个结构。
**为什么不主动探活**：探活方要维护 N×N 探测关系，且"探不通"分不清对方死了还是网络分区。自上报把判断压缩成一次时间戳比较。
**时钟必须是单一共享源**（这是最容易被追问的点）：判活是"拿 A 机器写的时间戳和 B 机器的现在比"。各用本地时钟，机器间时钟偏移就**直接变成误判**——快 10 秒的节点看谁都像过期，慢 10 秒的节点写的心跳一出生就过期。我用 Redis `TIME` 命令作为集群唯一时钟。
**阈值是不对称代价的取舍**：`TTL = 3 × 心跳间隔`（2s → 6s）。误摘活节点 → 它名下房间被别人认领，而它内存里还握着状态 → **双写**；这比晚几秒发现宕机严重得多，所以宁可保守。
**为什么用 ZSET**：member = node_id，score = 最后心跳。纯 SET 没有成员级 TTL，死节点永远留着；每节点一个 TTL key 能自动过期，但列举存活节点要 SCAN 整个 keyspace（生产慢操作）。ZSET 一次 `ZRANGEBYSCORE` 取存活集合，一次 `ZREMRANGEBYSCORE` 清死节点。延时队列、滑动窗口限流是同一个套路。
**加分细节**：身份表和地址表分开存——node_id 决定房间归属（进环），地址只是转发用的联系方式，节点换 IP 不该引起房间迁移。
**关联**：`backend/app/core/node_registry.py`，DEVLOG C06。

### Q26 请求落到非归属节点怎么处理？转发会不会成环？
**骨架**：转发，且**一跳封顶**。
**为什么必须转发不能就地处理**：就地处理意味着本节点从快照重建一份状态，于是同一房间有两份内存状态各自演进，单写者模型作废。路由的意义就是保证"一个房间的写入永远只发生在一个进程里"。
**环从哪来**：两节点的存活视图在心跳间隙可能短暂不一致——A 认为归 B，B 认为归 A，不设防就是 A→B→A→B 无限转发，一次请求打爆两个节点。
**解法**：转发时打 `X-Room-Forwarded` 头，收到带该头的请求**只在本地处理，绝不二次转发**。代价是这一次请求可能落在非归属节点上（视图收敛后自愈），收益是**环在结构上不可能出现**——这是用"极小概率的一次错误落点"换"绝对不会打爆集群"。
**追问预案**："限流怎么处理？"——转发后的请求会在第二个节点再过一次限流器，用户配额凭空减半，且计几次取决于房间落在哪个节点，行为不可预期。所以限流只在**入口节点**算一次，带转发标记的请求豁免。这不构成绕过途径：标记只在集群内部转发时添加，外部请求即使伪造该头也已在入口节点被计过一次。
**关联**：`backend/app/core/room_router.py`，`app/core/rate_limit.py::SkipForwardedRateLimiter`。

### Q27 接路由时踩过什么坑？（讲一个多节点专属 bug）
**骨架**：三个坑，共同点是**单机跑不出来、而且都不报错**。这也是我第一步只写哈希环纯算法 + 单测、故意不接请求路径的原因。
1. **转发头白名单漏了一个鉴权头**。`forward()` 只透传 `authorization`，但 AI worker 的内部接口用的是 `x-internal-secret`，转发时被丢掉 → 归属节点固定 403 → AI 玩家全部动不了 → 对局停在 AI 回合 → 人类玩家永远等不到自己的回合。**症状出现在人类的提交路径，病灶在 AI 的提交路径**。白名单转发的固有成本就是这个：漏一个头不报错，只让某条端点在转发后固定失败，所以约束必须写进代码注释而不是指望下次记得。
2. **只接了写路径，读路径没接**。读接口读的是进程本地字典。建局发生在 node-1、房间归 node-2，node-1 手里就留着一份**建局那一刻的初始状态副本且永不更新**。客户端从 node-1 轮询，永远看到第 1 轮，永远算出"没轮到我"。教训：**无状态服务里"读打到哪台都一样"的直觉，在单写者模型下是错的**——"只路由写"是个看起来合理、实际半残的方案。
3. **非归属节点留下的内存副本会让对局回退**。建局节点无条件缓存了状态，等房间日后迁到它名下，动作处理会因为"内存已有"而**跳过快照恢复**，照着建局时的旧状态继续演进。根因是"内存里有就用内存"这条捷径的隐含前提变了：从"本进程是唯一写者"变成"本进程是**这个房间的**归属节点"。修法是读路径回落快照但**刻意不回写内存**，且只在 `is_mine` 时才缓存。
**共同经验**：三个都返回 200、数据结构合法，只是内容是旧的。这类问题不会被状态码监控发现，得靠业务不变量（"轮次只能前进"）才能兜住。
**关联**：DEVLOG 016~018。

### Q28 房间迁移怎么验证？拿什么当判据？
**骨架**：判据要能证伪。"动作提交返回 200"证明不了任何事——转发失败、就地用旧状态处理，都可能返回 200。
**我用的判据**：加一个 `/cluster` 端点暴露本节点身份、存活节点集合、**本地驻留的房间列表**。断言变成"这个房间只出现在归属节点的驻留列表里，入口节点为 0"——这同时证明了转发生效**和**状态没有分叉。
**转发正确性实测**：房间归 node-2，**全程从 node-1 提交** 6 个动作，对局正常推进到第 3 轮，房间只驻留在 node-2。200 个房间的归属判定在两节点上完全一致，0 分歧。
**故障转移实测**：房间归 node-2，打到第 2 轮任务阶段（19 条发言）→ `kill -9` node-2 → 幸存节点 **5.6s 完成判死与接管**（TTL = 3 × 2s = 6s，符合预期）→ 从 Redis 快照恢复后续打 5 个动作到第 4 轮（39 条发言），**状态无回退**。
**加分细节**：断言里显式检查"迁移后发言数不小于迁移前"——回退是这类 bug 最典型的表现，而它不会报错。
**关联**：`bench/drill_room_routing.py`（`verify` / `takeover` 两段）。

## 长连接与广播（E 组）

### Q11 长连接服务的容量上限由什么决定？怎么排查？
**骨架**：每连接持有成本审计——FD、内存缓冲区、线程、DB 连接，任何一项被独占都是上限。真实案例：我们的 WS 鉴权用 FastAPI yield 依赖注入 DB Session，连接存续期间独占池连接，默认池 15 个 → 第 16 条 WS 连接开始排队 → 线程池堵死 → accept 积压 → 整个服务（含健康检查）雪崩。修复：握手鉴权改短生命周期 Session。修复前 100 连接 93% 失败，修复后 1000 连接 0 失败（DEVLOG 006）。
**追问预案**："怎么定位的？"——事件循环空闲 + TCP 超时 = accept 积压，逆推线程池 → 连接池；日志里 QueuePool TimeoutError 是实锤。排除法先排除新引入的中间件（A/B 复测）。

## 缓存与热榜（F/G 组）
（待 F/G 组完成时补充）

## 韧性工程（H 组）
（待 H 组完成时补充）
