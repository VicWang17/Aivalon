# Todo v2｜Aivalon 高并发架构升级

> 对应文档：`AGENTS.md`（工作守则）、`document/DEVLOG.md`（排障与概念）、`interview_qa.md`（面试答题）
> **唯一范围基准**：简历 Aivalon 七条描述。**简历没写的不做**；简历写了但没做的就是待偿债务。
> **执行方针**：只求做出来，效率第一。不做接口抽象类重构、不做简历不提的周边功能、不做额外压测报告。
> **数字纪律**：简历已有实测出处的数字不再复测；未做的功能做完按实测改简历（第一铁律），测不出来就改简历。

---

## 🔄 当前位置（2026-08-12 下午）

**A / B / C / D / E / F / G 组已全部完成。只剩 H（韧性）,全部是"简历已写成事实但代码没有"的功能。**

已完成：可观测体系、压测平台 + v1 基线、测试安全网（332 项全绿）、房间 Actor 单写者、Write-Behind、一致性哈希路由 + 心跳租约 + 故障转移、动作 P99 口径复测、WS 网关拆分 + 跨节点扇出 + 合并帧 + 分级推送 + 背压、L1/L2 两级缓存 + 事件驱动失效 + 布隆防穿透 + singleflight 防击穿 + 逻辑过期防雪崩、热榜批量合并 ZINCRBY 写路径 + 分片 ZSET 定时归并读路径。

**排障记录索引**（细节在 `document/DEVLOG.md`）：
- 006 WS 鉴权 yield 依赖独占连接 → 100 连接 93% 失败改到 1000 连接零失败
- 011 → 012 同一个病修两次；012 的"持有并等待"自死锁是 011 的修复引入的
- 013 kill -9 恢复演练 + RPO 量化。**边界：`restore_game_state` 从 DB 重建的分支仍是 TODO,恢复依赖 Redis 快照存在**
- 014 `INSERT IGNORE` 静默吞外键失败 → 全库 372 局 `GAME_START` 事件 0 条
- 015 写事务数 -78~81%（口径是事务数不是行数,行数 0%）
- 016~018 三个多节点专属 bug（转发头白名单 / 只接写路径 / 非归属节点内存副本致对局回退）
- 019 动作 P99 两轮差三倍是分位数口径错,不是性能问题
- C05 一致性哈希与哈希盐陷阱 / C06 心跳租约判死 / C07 分位数的样本量前提

**遗留（已知不修）**：`auth.py` 的 `register` 是 `async def` 里做同步 `db.query`,不在热路径。（原来挂在这里的 `room_actor.py` Actor 空闲退出竞态与 `actor.submit` 无超时,已随 H-3c 资源层限流一并结清,见 DEVLOG 037。）

**环境启动**：
```bash
docker-compose up -d
cd backend && source venv/bin/activate
export AI_USE_LLM=false AI_TASK_RATE_LIMIT=100000/m RATE_LIMIT_ACTION_TIMES=100 RATE_LIMIT_CREATE_GAME_TIMES=10000
uvicorn app.main:app --port 8000 &          # 确认 pgrep -f "uvicorn app.main" | wc -l == 1
celery -A app.core.celery_app worker --loglevel=warning &
python -m app.core.outbox_relay &
./run_tests.sh                              # 应 332 项全绿
```
多节点：`NODE_ID=node-2 NODE_ADDR=http://127.0.0.1:8002 uvicorn app.main:app --port 8002`,演练 `bench/drill_room_routing.py verify|takeover`。

**关键文件**：`room_actor.py`（Actor）/ `event_journal.py` + `event_flusher.py`（Write-Behind）/ `hash_ring.py` + `node_registry.py` + `room_router.py`（路由）/ `socket_manager.py`（广播,E 组入口）/ `bench/results/`（数字出处）

---

## 📋 简历欠账表（只列没结清的）

| 段 | 简历原话 | 欠什么 |
|---|---|---|
| ③ | 创建对局耗时…降至 **226ms** | ⚠️ 226ms 是 max,无统计稳定性（复测两轮 187ms / 411ms）。**建议改成"降至亚秒级"**——不需要重测,只需改措辞（DEVLOG 019 / C07） |
| ⑤ | 独立 WS 网关分离连接与逻辑,Redis 路由表跨节点扇出；同 Tick 事件合并帧、分级推送、慢消费者背压 | E 组全部未做。⚠️ 2,000 连接与 180ms 是**这些优化之前**测出来的,别把因果挂上去 |
| ⑥ | 热榜**分片 ZSET + 定时归并 Top N + 本地缓存** | ✅ 已结清。缓存半段（L1+L2 / 事件驱动失效 / 布隆 / singleflight / 逻辑过期）、MQ 批量 ZINCRBY 写路径、分片 ZSET + 定时归并 + L1 读路径全部到位（DEVLOG 025~032）。⚠️ 边界：**单实例 Redis 下分片本身没有可报的吞吐数字**,它买的是"横向扩得动"和"没有单点热 key",别把它说成当下的性能提升 |
| ⑦ | 分层限流 + 5 级降级矩阵 + 依赖熔断,**10 倍突发流量下核心对局链路零中断** | H 组全部未做。⚠️ **"零中断"与已归档基线直接矛盾**：`v1_baseline.md` S4 记录 10 倍突发失败率 10.09%、P90 恶化到 7.8s。基线报告在 GitHub 上,面试官翻得到——这是唯一必须实测的一个数,做完 H 组跑一次 S4,拿不到就改简历 |

已结清的数字（出处在 DEVLOG / `bench/results/`,不再复测）：2,000 长连接、广播 P99 180ms、动作 P99 83.8ms、4~5 事件合并一次提交、写事务数 -80%、RPO 200ms、一致性哈希 + 160 虚拟节点、迁移量 3/4 → 1/N、节点宕机 5.6s 接管、状态无回退、100 → 2,000 连接。

---

## A / B / C / D 组 ✅ 全部完成

- [x] A 可观测：Prometheus + Grafana + 业务指标 + trace_id 透传 + 连接池探针 + `/cluster` 集群视图
- [x] B 压测：Locust 工具链 + S1~S5 五类场景 + v1 基线报告归档 + 写压力量化脚本
- [x] C 安全网：状态机单测 + 主链路集成测试 + 哈希环/路由单测,`run_tests.sh` 54 项
- [x] D 热路径：Actor 单写者去锁 + 空闲卸载；一致性哈希路由（160 虚拟节点）+ 心跳租约 + 读写双路由转发 + 一跳防环；节点宕机房间漂移 + 快照恢复；Write-Behind（Redis Stream + 200ms 批量刷库）+ RPO 量化 + kill 演练 + 写事务量化；动作 P99 口径修正

---

## E. 长连接与广播（简历⑤）

- [x] WS 网关拆成独立服务：`app/gateway.py` 独立入口,复用 `ws.router` 只挂 WS 一个路由,不进一致性哈希环（进环会把 1/N 房间分给没有业务逻辑的进程）；`ws.py` 去掉 `GameService` 依赖,推送等级改由 `core/ws_tier.py` 只读 Redis 快照判定；职责边界用 AST 断言钉住（DEVLOG 024）
- [x] 连接 → 网关节点映射存 Redis（简历说的"Redis 路由表"）：房间 → 持有其连接的节点集合,节点粒度登记,TTL 兜底崩溃残留
- [x] 跨节点扇出：查路由表定向 publish 到目标节点专属频道（不广播全集群）,本节点直发不绕 Redis,收到转发只做本地投递防环。11 项测试（DEVLOG 020）
- [x] 同 Tick 事件合并帧：50ms 合并窗口内同房间多个 STATE_UPDATE 只下发最后一帧,合并率看 `ws_frames_merged / ws_frames_sent`（DEVLOG 021）
- [x] 分级推送：`unicast` 操作者单播（跨节点按 user_id 筛）/ 玩家 50ms 即时 / 旁观者 500ms 聚合成一个 BATCH 帧；角色按"是否占座"服务端判定,不信客户端声明（DEVLOG 023）
- [x] 慢消费者背压：每连接独立写缓冲 + 写协程,广播不再 await socket（消除队头阻塞）；缓冲让出一次事件循环后仍满则以 1013 断开,指标 `ws_slow_consumers_dropped`（DEVLOG 022）

## F. 缓存（简历⑥·上）

- [x] L1 进程内 + L2 Redis 两级缓存,未命中回源 MySQL：`core/cache.py`,加在**回放事件流**读路径（全站唯一真回源 MySQL 的接口,且事件只增不改）。**刻意不加在对局状态路径**——那里的 `games` 是 Actor 权威状态,非归属节点留副本会重现 DEVLOG 018 对局回退。L1 有条数上限 + TTL 即一致性上限,指标 `cache_reads{level}`（DEVLOG 025）
- [x] 事件驱动失效 + TTL 兜底：Pub/Sub 失效通道补上 L1 跨进程失效不了的洞（L2 共享删一次即可,L1 在各进程堆里得喊一声）。**失效点挂在 flusher 刷库成功之后**,不在动作发生时——事件走 Write-Behind,提前失效会让读回源到还没刷入的 MySQL、把旧结果重新缓存 300s。失效失败不上抛,L1 短 TTL 才是保证（DEVLOG 026）
- [x] 穿透：布隆过滤器拦截不存在的 game_id：`core/bloom.py`，md5 双哈希（内置 `hash()` 受 `PYTHONHASHSEED` 影响，跨进程算出不同位 → 共享位图失效，同 C05）。**能当门卫全靠误判单向**：只会误判"存在"，绝不误判"不存在"。三条工程约定维持"无假阴性"：空位图放行 / key 不设 TTL / 启动时位图不存在则灌一遍库里已有 id——少一条就会把真实房间 404 掉。拦截点放在**跨节点转发之前**（省掉一次往返），刻意不拦 `load_game`（AI 任务与快照恢复走它）。Redis 故障一律放行，指标 `bloom_rejects`（DEVLOG 027）
- [x] 击穿：singleflight 互斥回源：`cache.py` 的 `_load_once`，`_inflight` 存 `key → future`。**用 future 不用锁**：锁只保证不同时查，后来者拿到锁还会各自再查一次；future 直接把第一次的结果交给所有等待者，N 个并发 → 1 次查询。两个真坑：① `asyncio.shield` 挡取消传播——不加的话一个等待者被取消会连带打断回源方和其他等待者，**收敛故障的机制反而放大故障**；回源方被取消则必须放掉等待者，所以捕获 `BaseException` 而非 `Exception`（`CancelledError` 不是 `Exception` 子类）。② 占位前必须二次检查 L1——L2 读那次 `await` 期间别的任务足够跑完一次回源并回填。互斥按 key 不按全局。边界：**进程内互斥不是集群级**，M 个进程最坏 M 次回源；不上分布式锁是因为它要在每次未命中路径上多付一次往返，而把 N 压到 1 是数量级改善、把 M 压到 1 只是常数级。10 项单测，`gather` 50 并发断言只回源 1 次，不用压测（DEVLOG 029）
- [x] 雪崩：逻辑过期异步重建 + TTL 抖动：L2 存的是信封 `{"v": 值, "exp": 逻辑过期时刻}`，物理 TTL = 逻辑过期 + 60s 宽限窗口 + ±10% 抖动。读到逻辑过期的值**先返回旧值、重建放后台**，调用方一次也不等（singleflight 把 N 次回源压成 1 次，但那 1 次还是有人在等）。抖动治的是**雪崩会自我强化**——第一轮集体到期后幸存的 key 全被对齐到同一时刻，下一轮更整齐。两个坑：① F-4 那个二次检查 L1 会让重建空转（旧值刚被填进 L1），所以重建走 `skip_l1` 但照旧查 `_inflight`；② `_decode` 返回三元组而非二元组——**None 是合法缓存值**，拿它当解析失败会把 F-3 的空结果缓存废掉。`KEY_PREFIX` 升 v2（值结构变了必须换前缀，这是 F-1 留它的用处）。13 项单测，核心那条判耗时不判返回值（DEVLOG 030）

## G. 热榜（简历⑥·下）

- [x] 写路径：对局结束 → MQ（已有 Celery `stats_queue`）→ 批量合并 ZINCRBY：`core/rank_buffer.py`，增量先 `HINCRBY` 攒进 HASH（field = `榜|玩家`），后台循环每秒 `RENAME` 换出 + pipeline 批量 `ZINCRBY`。**`ZADD` → `ZINCRBY` 省掉的不是一条命令而是那次查库**：`ZADD` 写绝对值就必须先知道现在是多少（那次 MySQL 查询的来源），`ZINCRBY` 写增量而增量在对局结束那刻已经知道。代价是不幂等，接受 at-least-once——榜是可以从 MySQL 全量重建的派生数据。收益分两笔别混着吹：**批量化是主要收益**（N 次往返变 1 次），同 member 合并在本项目里不大（一个人没法同时结束两局），比值看 `rank_updates_buffered / rank_updates_applied`。三个坑：① 换出必须 `RENAME` 不能"`HGETALL` 后 `DEL`"，后者会把两步之间进来的写连带抹掉且无从发现；② `RENAME` 的原子性顺带做掉多节点互斥，不用再套分布式锁；③ 删除放在 `ZINCRBY` 之后（宕机则重放一次，可自愈），反过来是永久少算。顺手修了既有 bug：`stats.py` 拿大写名字的集合比小写枚举值，`is_evil` 永远 False——坏人赢了不加分、好人赢了全场都加分，**不报错只是全算错**；判据收敛到 `game_enums.camp_of()`。13 项单测（DEVLOG 031）
- [x] 读路径：分片 ZSET + 定时归并 Top N + L1 本地缓存：每榜拆 8 片（`rank_buffer.shard_of`，md5 不用内置 `hash()`——写入进程和归并进程不是同一个，同 C05 盐坑），归并循环每 5s 取各片 Top N 排序、**连展示字段一起烤进快照 key**，读接口只读快照。**按 member 哈希分片而不是按写入轮转**：一个人的分数完整待在一片里，所以全局 Top N ⊆ 各片 Top N 的并集，归并只读 `SHARDS × limit`（8×10=80）行、**上界与总人数无关**；轮转分片会把同一个人拆散在多片，必须全表扫才能排序——**分片键约束的是查询形态**。不用 `ZUNIONSTORE`（要并全部成员，开销随人数走且堵住单线程 Redis），改在应用侧排序。原来"查 Redis 排序 → 拿 id 回 MySQL 查用户名场次"等于**榜单 QPS 直接压在库上**，现在读路径一次都不查库，查库降为每 5s 最多一次（口径 `rank_reads{result}`：`merged` 跟着读 QPS 涨就说明归并循环没跑）。快照**按最大 limit 存一份**、切片在应用侧（`limit` 是无界维度，按它分别存就是让 key 跟请求参数爆开，同 C02）。`get_or_load` 传 `redis=None` 只复用 L1 + singleflight（快照本身就是 L2）。归并循环**刻意不互斥**（纯读+覆盖写，重复只费 CPU），和写路径 `RENAME` 必须互斥恰好相反——**看重复执行会不会改变结果，不看是不是"后台任务"**。16 项单测（DEVLOG 032）

## H. 韧性（简历⑦）

- [x] **AI 降级开关运行时可切**（L1/L2 的前置）：`core/switches.py`,开关值存 Redis(`switch:ai_use_llm`),各进程读同一个 key、本地带 1s TTL 缓存,操作入口 `/internal/switches`（GET/POST/DELETE,`X-Internal-Secret` 鉴权,不进 OpenAPI）。原来 `AI_USE_LLM` 启动时读一次,要改就得重启所有 Celery worker,而 AI 决策就跑在 worker 里——**需要重启才能生效的降级开关在事故里等于没有**。两个 TTL 方向相反：**开关本身不设 TTL**（降级是人做的决定,不能自己弹回来,半夜降完睡觉 TTL 一到故障重来一遍),**本地缓存必须设 TTL**（开关每个 AI 回合读一次,不然就是把 Redis 挂在热路径上;TTL 即一致性上限)。**失败方向：Redis 读失败优先保持本进程上次读到的值,哪怕已过期**,只有从没读到过才退回配置默认值——切降级往往正因为线上在着火,而 Redis 抖动本身就是着火的一部分,退回默认值等于开关在最需要它的时刻自己失效;这和缓存"读不到就放行回源"恰好相反,**一个是"可以慢"另一个是"不能变"**。默认值仍取自 `settings.AI_USE_LLM`,压测那套环境变量照旧有效。开关状态上指标 `aivalon_degrade_switch{name}`（降级动作不可观测就是静默变更)。11 项单测（DEVLOG 033）
- [x] **LLM 舱壁**：`asyncio.wait_for` 包住整次调用 + `max_retries=0`,超时/异常一律返回 `{"error": ...}` 回落规则引擎。原来把 `timeout` 直接传给 OpenAI SDK,看着像"最多等 45 秒"——实际 SDK 的 `timeout` 是**单次尝试**的、默认还重试 2 次、重试间有退避,真实上界 140 秒以上。**"我传了 timeout"和"这次调用有上界"是两回事**,上界要卡在自己代码里、不能外包给依赖库的参数（`wait_for` 在最外层,SDK 换版本这个上界还成立）。**有规则引擎兜底时重试的价值是负的**——它花掉的是玩家的等待时间,所以关掉 SDK 重试。**超时绝不上抛**：抛出去走 Celery 重试,而重试打的还是那个慢掉的 LLM,一次慢放大成五次慢。`asyncio.TimeoutError` 分支必须排在 `except Exception` 前面（3.11 起它就是内置 `TimeoutError`,而那是 `OSError` 子类,会被 `except Exception` 抓走;写反不报错、只是超时全记进 error 档,**指标静默失真**）。顺手接上了 `llm_calls_total` / `llm_latency`——它们声明在 `metrics.py` 但全代码库从没 `inc()` 过。指标口径两条：**失败和超时的耗时也要记**（只记成功的话 LLM 全超时时曲线反而变好看,最该告警的时刻一片绿),**四档分开**`success`/`timeout`（依赖慢了→降级扩容)/`invalid`（依赖坏了,通了但非 JSON→改 prompt)/`error`（鉴权配额网络)。超时值提到配置 `AI_LLM_TIMEOUT_SPEECH=45` / `AI_LLM_TIMEOUT_ACTION=20`,事故里可压小。11 项单测（DEVLOG 034）
- 分层限流（三层，分开做）：
  - [x] **网关层准入：全局令牌桶 + IP 令牌桶**。`core/admission.py` + `AdmissionMiddleware`,挂最外层,被拒的请求不碰任何后端资源。和已有 `rate_limit.py`（按 user_id 的业务规则）是两层不同的东西——**一万个用户每人只建 1 局完全合规,机器照样倒**,按用户限流保护不了系统容量。不复用固定窗口的两个理由：窗口边界能挤进 **2 倍**流量;更关键是固定窗口只有"N 次/秒"一个旋钮,**表达不了"允许突发到 400、稳态只给 200/s"**,而这正是真实流量的形状。令牌桶把它拆成两个独立旋钮：`capacity`=突发上限,`rate`=稳态速率。**必须用 Lua**：「读桶→算补令牌→判断→写回」是读改写序列,多进程共享一个桶,`GET`+`SET` 会让两个进程同时读到"还剩 1 个"各自放行。**时间基准取 `redis.call('TIME')` 不取节点本地时钟**——节点间时钟偏差会直接变成令牌数偏差,快的那台等于给自己多发配额（**共享状态的时间基准也是共享状态**,同 C05）。**失败方向：Redis 读失败一律放行**,限流器是保护机制不是可用性依赖,抖一下就拒全站的话它自己成了新单点、造成的故障比防的还大——这和 H-1 开关"读不到保持降级态"**恰好相反**。三处"限流器自己变成放大器"的坑：`/health`+`/metrics` 必须豁免（拒了健康检查→LB 摘节点→剩下的更快被拒→**限流器亲手把过载放大成雪崩**）、429 必须带 `Retry-After`（不说等多久客户端就立刻重试,重试本身变成新峰值）、**被 IP 层拒掉的不能扣全局桶**（否则刷接口的 IP 顺带烧掉别人配额,而那正是它想干的）。**不读 `X-Forwarded-For`**（可伪造的限流键等于没有限流键）。中间件顺序刻意让 TraceId 包在外层,429 也要有 trace_id 和访问日志。桶 TTL 按 `cap/rate`（恰好攒满)设,无损——**过期消失的桶和满桶完全等价**,顺手解决 IP 桶随访客数无限增长。指标 `aivalon_admission_rejects_total{layer}`,global/ip 分开（**看到它们要做的事不同**：global=系统到顶该扩容,ip=某来源在打我该封）。阈值暂拍（全局 400/200,单 IP 60/20),等 S4 出真实拐点再定。12 项单测（DEVLOG 035）
  - [x] **应用层：固定窗口换滑动窗口 + 补齐遗漏路由**。新写 `core/sliding_window.py`（ZSET 滑动窗口日志),**所有**限流器统一到它上面,`rate_limit.py` 退化成配置入口（各接口 scope 与阈值)——刻意统一不并存,**留两套限流机制比留任何一套都糟**,读代码得先判断这条路由走的是哪套才知道边界行为。换算法的理由：`fastapi-limiter` 的 Lua 是 `SET px`+`INCR` 即**固定窗口**,key 从第一个请求起算过期、到点整批清零,于是**上小时最后一秒建 10 局 + 下小时第一秒再建 10 局 = 2 秒内 20 局**,而每次都"没超过 10 局/小时"——**业务承诺被算法实现细节打了个对折**。顺手修掉一个真漏洞：`default_identifier` **读 `X-Forwarded-For`**（客户端可随便填),而 `send-code`/`login` 原来没传 identifier 用的正是它——**最需要限流的两个接口限流键可以伪造**,换个假 IP 就能无限发验证码（每封花真钱)、无限撞库。覆盖面也补齐：原来只有建局+提交动作两个路由,现在加上 register、榜单、回放、历史、对局快照,并按"这个操作多贵"和"真人能多快"分档（发码 3/小时、登录 10/5 分钟、注册 5/小时、读接口 60/分)——**不是所有接口配一个数**。两层用不同算法是因为要的性质不同：网关层要突发与稳态分开调（令牌桶两个旋钮),应用层要**没有可利用的边界**（承载业务承诺)。滑动窗口代价是内存 O(窗口内请求数),但**先 trim 再计数、超了不写**,所以 ZSET 上界就是 `times` 本身;这也划定适用范围——`times` 小时划算,`times` 很大（网关 200/s)就该用令牌桶（**只存 2 个数字,与流量无关**)。三个易错细节：**trim 必须在计数前**（先计数会把已滑出的算进来,且不 trim 则 ZSET 无限长大)、**超限时刻意不写入**（写了则每次被拒都把窗口往后推,**重试的客户端把自己永久锁死**)、**member 用 uuid 不用时间戳**（member 相同会被 ZSET 覆盖成一条,**并发越高漏计越多**,限流器恰在压力最大时失效);score 才是跨进程比较基准,取 `redis.call('TIME')`。指标 `aivalon_rate_limit_rejects_total{scope}`（scope 低基数可进 label,user_id 绝不能进,C02)。15 项单测（DEVLOG 036）
  - [x] **资源层·上：单房间事件风暴保护**。`room_actor.py` 上加两条上界——队列满抛 `RoomOverloaded`（路由层转 503 + `Retry-After`）、等待超时抛 `RoomActionTimeout`（转 504）。前两层统计不到这一类：**8 个人对着同一局猛点，每个人都没超自己的 1 次/秒，那个房间的队列却能排到几千个**，而队尾的人要等前面全部跑完——按用户按 IP 都看不见，只有**持有资源的地方**看得见。**先判满再入队，刻意不用有界 Queue 的 `await put`**：那是让提交方排在队列外面等，队伍只是从看得见的地方挪到看不见的地方，延迟照样无上限;过载时最不该做的就是让调用方多等，**等待本身就是资源**。最要紧的一条：**超时后还没出队的动作必须丢弃**（`wait_for` 取消 future 恰好提供了"提交方还在不在"这个信号，所以别换成 `shield`），否则客户端按超时重试一次、服务端把排队那次也跑了，**一个动作生效两遍**，投票提名重复执行会直接把对局算错;这也是队列上界的另一半价值——**过载时队里堆的多半都是已经没人等的动作**。两种拒绝**刻意分开不合并**：`queue_full` 压根没入队所以**一定没生效**可安全重试（503），`timeout` **可能已经在跑、服务端无法断言**（504，客户端该先拉状态再决定）——**错误码要如实反映"这次到底做没做"**。顺手结清两条已知欠账：空闲判定与新动作入队撞车（`wait_for` 取消了 `get()` 后 `put_nowait` 进来的动作没人取、future 永不完成，原来是永久挂起）、注销必须**比对身份不能按 game_id 盲删**（老 Actor 退出回调晚于新 Actor 建好会删掉正在用的那个，同一房间短暂两个写者，**单写者模型就破了**）。指标 `aivalon_room_overload_total{reason}`（game_id 绝不进 label，C02）。11 项单测（DEVLOG 037）
  - [x] **资源层·下：AI 队列深度上限 → 自动降级**。`core/ai_queue.py`：投递侧 `enter()` 登记"在飞任务"、token 随 Celery 任务带下去，worker 跑完注销；worker 执行前读一次深度，超阈值就 `get_action(force_fallback=True)` 摘掉 LLM 走规则引擎。**这一层过载时不能拒**——前面三层（拒 HTTP / 拒建局 / 拒动作）都有个共同前提：**对面有客户端、它会拿到错误码会重试**；AI 回合没有这个前提，被丢掉就没有任何人会再提交它，**房间不是变慢而是永久卡死**。所以"限流的手段是拒绝，但过载控制的目的不是拒绝"，判据很具体：**对面有没有一个会重试的客户端**。**降级降的是"每个任务多贵"不是"放几个进来"**：同样长的队列，走 LLM 每条十几秒、走规则引擎每条毫秒级——**队列长度一点没变，排空时间差三个数量级**,而且对玩家的可见损失最小（对局照常，只是 AI 说话变套路）；这就是 5 级矩阵的 L2，只不过按深度自动触发。**记账在投递侧**（等 worker 领到才记的话，积压期间躺在队列里的一个都不算，深度永远看着很小），**判断在 worker 里**（投递到执行隔着排队，用投递那刻的深度等于拿几十秒前的旧情况做现在的决定）。**刻意不问 broker 要 message_count**：那是一次阻塞 AMQP 往返，加在每个 AI 回合上就是把 broker 挂进热路径——**保护机制自己成了新的延迟来源**。**关键选型：ZSET 存"在飞任务 → 投递时刻" + 租约清理，不用 INCR/DECR**——worker 被 kill -9 不会注销，纯计数器**误差只会单向累积**，攒够阈值后系统**永久停在降级态**且从曲线上分不清是漏账还是真积压;**凡是"登记后靠对方注销"的计数都必须假设对方不会来**（同 C06）。租约有下界（必须 > LLM 舱壁 45s，取 120s）：小了会把**正在跑的**任务当漏账清掉，深度永远上不去、**降级永不触发且不报错**——测试钉的是这个关系不是具体数值。**失败方向：读不到深度报 0 = 不降级**（自动推断出来的降级，推断不出来就别擅自降；误降是全站说套话，不降有 H-2 舱壁兜着——**哪边代价可恢复就往哪边倒**）。这是本项目**第四种失败方向，四种全不一样**：缓存放行回源 / 开关保持现状 / 限流器放行 / 自动降级不降。**自动降级与人手开关刻意两个入口**（`force_fallback` vs `switches`）：一个每回合重判、一个要留到人来撤销，**生命周期不同的东西不能共用一个状态位**,混一起就是"人的决定被机器覆盖"或"机器的决定撤不回去"。重试中的任务不注销（重试的任务恰恰是积压的一部分）；任务签名加参数**必须给默认值**（队列里躺着上一版投递的消息,少一个参数就是一批 TypeError 进死信队列）。顺手接上从没 `set()` 过的 `ai_queue_depth`,新增 `aivalon_ai_turns_degraded_total{reason}`（**自动降级尤其必须可观测：没人按过按钮**）。16 项单测（DEVLOG 038）
- [x] **5 级降级矩阵 + 一键开关中心**：`core/degrade.py`,档位存 Redis(`degrade:level`)、本地 1s TTL 缓存,入口 `/internal/degrade`（GET 带整张矩阵 / POST 拧档 / DELETE 一键回 0,`X-Internal-Secret`,不进 OpenAPI）。L1 只砍 AI 发言 / L2 AI 全决策走规则引擎 / L3 冷路径降频 / L4 建局排队 / L5 拒新开局。**刻意是一个整数旋钮不是 5 个复选框**：5 个复选框要求操作的人在压力下自己记住依赖顺序（先关发言还是先关热榜?关了 L4 要不要顺手关 L2?),而矩阵的本质是**这些措施有序且累积**——用一个数字表达是精确的,用 5 个布尔则允许"L3 开着但 L1 关着"这种没意义的组合。**最容易写错的一条：判定必须 `>=` 不能 `==`**——`if level == 2` 在升到 3 档时会把 LLM 自己开回来,**往更严重的方向拧,反而恢复了更贵的功能**。**档位排序依据是"这一刀砍多少成本"除以"玩家多疼"**：AI 发言排第一（最贵的一次 LLM 调用,输出长温度高超时上界 45s 是投票两倍多,且说什么都不改变胜负）,"不让人进来玩"排最后;排错顺序就是降了一级没省到成本、只好继续往上拧。**L3 是降频不是关掉**（榜单晚十几秒没人投诉,**查不到榜单会被当成故障**),所以 `cold_path_interval()` 返回间隔而非布尔,热榜归并与回放两级 TTL 都乘倍数——TTL 即一致性上限,花掉的是"数据可以多旧";归并循环里**每轮重读档位**,循环外读一次就等于要重启才生效（同 H-1）。**L4 的配额刻意是全局的不是按用户的**：按用户那个（10 局/小时）一直在生效但保护不了系统容量（一万人每人 1 局全合规,机器照样倒,同 H-3a),L4 压的是"全站每秒开几局",复用 H-3b 滑动窗口。**L4/L5 的差别不只是数字大小,是给客户端的语义不同**：L4 = 429 + `Retry-After`（等等能建,该排队重试),L5 = 503（这功能整个关了）;L4 配额**必须是正数**,配 0 就等于 L5、这一级白设。**L5 的闸只挂建局不挂对局内动作**——"只服务进行中的房间"全部含义就在这里,**挡错地方就不是降级而是故障**,用 AST 断言钉住调用点。**越界报错刻意不夹逼**（手滑输入 50 夹成 5 就是静默把全站新开局拒了,**夹逼是替用户猜意图而这里猜错的代价是全站不可用**）。**"读不到"与"读到脏值"失败方向相反**：前者保持上次档位（人做的决定,同 H-1),后者按 0 档（宁可不降也别因一个坏值把全站拒了）。AI 侧四条通路取"或"（`force_fallback` / 人手开关 / L2 / L1）——**谁都不该被别人的"不用降"覆盖掉**,但 reason 分开上报（要人做的事不同）。**降级拒绝与限流拒绝分开计数**：同一个 429 背后是"有人打得太猛"和"我们自己关的闸"两件事。**恢复必须和降级一样一步到位**（要求逐级退就是要求人在最累时多做 5 次操作),下调多级只打 warning、不在代码里强制逐级。指标 `aivalon_degrade_level`（一个 Gauge 不是每级一个,档位累积、要的是"何时拧到几档"这一条线）+ `aivalon_degrade_rejects_total{reason}`。24 项单测（DEVLOG 039）
- 依赖熔断（按依赖的失败形态分开做）：
  - [x] **LLM 熔断**：`core/breaker.py`（滑动窗口失败比例 + 半开单探针）,挂在 `llm_service.generate_response` 里,只读接口 `/internal/breakers`。**已经有 H-2 舱壁了为什么还要熔断**：舱壁保证**单次**有上界,但它每次都要**付满那个上界**才知道这次也不行——LLM 挂掉时每个 AI 回合照旧等满 45 秒再回落,对局能推进但每步慢 45 秒,**看指标全绿、体验和卡死一样**。**舱壁约束单次最坏值,熔断约束累计浪费**,所以第一条测试量的是"短路那次花了多少时间"而不是返回值。**熔断挂在依赖边界不挂在调用方**（挂调用方则下一个调 LLM 的人不知道要先问一句,防线只护住一条路径）;短路返回的形状和超时那条完全一样,调用方一行不用改。**最难一条：熔断器绝不写人手拧的那个档位**——`set_level(2)` 只有一行但错在两处：机器覆盖人的决定（人拧在 3 档,熔断写 2 就把 L3 撤了）、且**撤不回去**（恢复后写回 0 会抹掉人半夜拧的那一档,而"谁拧的"这个信息在一个整数里存不下）。所以两个来源各存各的、读时取 max（`degrade.effective_level`）——**生命周期不同的东西不能共用一个状态位**的第二次应用。**踩到的死锁：熔断器会被自己推断出的降级锁死**——跳闸顶到 L2,AI 路径读到 L2 就直接走规则引擎、**一次都不再调 LLM**,于是没人调 `allow()`、冷却期过了也没人推进状态,永远停在 open;**它推断出的降级挡住了它自己唯一的恢复途径**,解法是把"冷却满→半开"挪进 `_advance()` 并让所有读状态的入口都先走它。**非法 JSON 刻意记成成功**：依赖是活的、答得也快,只是答得不对（多半是 prompt 问题）,算成失败则**一个 prompt bug 把熔断跳开、而半开探测同样拿回非法 JSON,于是永远合不回来**——判据是**有没有白花时间**,不是"我拿到想要的了没有";`CancelledError` 两边都不记。**按窗口失败比例判不按"连续 N 次"**（依赖半死不活时偶尔成功一次就清零连续计数,**永远不跳**,而这恰是最常见的故障形态）,但比例判定**必须配最小样本数**（1 次里失败 1 次是 100%,少了它第一次抖动就摘掉 LLM,同 C07）。**半开只放一个探针**（全放过去则依赖刚起色就被积压流量打回原形,**恢复探测自己把依赖打挂**）,但**探针不回报也必须能放下一个**（漏 `record`/被取消会让它永久停在半开、再也不探测,同 ai_queue 租约与 C06);探针成功**必须清空窗口**（留着旧失败则下一次失败立刻重算到阈值,刚合上又跳开）,探针失败**立刻重新跳开**（半开只有一个样本,等 `min_samples` 就是永远不够）。**状态放进程内不放 Redis**：熔断的意义就是少打一次外部调用,放 Redis 等于为省一次 LLM 先付一次往返（**保护机制成了新的延迟来源**,同 H-1/ai_queue),代价是 M 个进程各自学,同 F-4 那笔取舍。**短路那次刻意不记耗时**（没发生的等待记进去会把 P99 拉绿,**依赖挂着时延迟曲线反而变好看**;和 H-2"失败耗时也要记"是同一陷阱的两面）。指标 `breaker_state{name}`（0/1/2 一个数值不是三个布尔）+ `breaker_trips_total`（**跳闸次数才是验收口径,不是 state**：state 是瞬时值,抓取间隔里跳闸又恢复完全看不到）+ `breaker_rejects_total` + `degrade_level_effective`（和人手那条分开：只有 `degrade_level` 高=有人拧了闸该问"还没恢复吗",`effective` 高于它=**没人拧过是熔断在压着**该去修依赖,**两件事下一步动作相反**）;`llm_calls_total` 加 `breaker_open` 档,**熔断生效后 timeout 应停涨、它接着涨,两个一起涨就说明没挂在真正的调用点上**。**刻意不给手动跳闸入口**（已有开关和降级矩阵,再加一个就是第三个达到同样效果的入口,**事故里没人知道该撤销哪一个**）。24 项单测（DEVLOG 040）
  - [x] **邮件熔断 + 统计刻意不熔断**：抄之前先问"熔断在这里到底买到什么",三个依赖的答案全不一样,其中一个是"不该装"。**邮件熔断买到的不是时间而是并发槽位和 SMTP 连接**——发信跑在 `BackgroundTask` 里没人等,但邮件服务挂掉时每个发码请求都占住一个协程和一条连接直到超时,而服务商给的配额只有个位数,**占满之后连正常的信也发不出去了**;所以验收断言的是"跳闸后 `send_message` 调用次数不再增长"而不是耗时——**同一个模式在不同依赖上省的东西不同,理由变了验收口径也得变**。**不是每个熔断器都该挂降级级别**：LLM 是 `implies_level=2`（AI 全走规则引擎正好就是 L2）,邮件是 **0**——矩阵每级砍的都是对局链路开销,而邮件不在那条链路上,**为了发不出验证码把全站往下拧一档是荒谬的**。**在依赖已知不可用时别向用户承诺**：发码接口原来先写验证码 + 60s 间隔再把发信丢后台,邮件挂着时用户**既收不到信又被那个 60s 锁在外面**,而响应写着"已发送";所以是**两道不同的闸**——`is_available()` 挡"承诺"（在两个 `set` 之前,503 + `Retry-After`,不写任何 key）,`allow()` 挡"调用"（挂在依赖边界上）,**承诺发生在调用之前,挡调用的闸救不了已发出的承诺**,顺序错了不报错所以用源码顺序钉住。顺手补邮件路径三个既有洞：**压根没超时**（`fm.send_message` 卡住就是永久卡住,而它在 API 事件循环里）、**它会抛而调用方是 `BackgroundTask`**（**没人能据此行动的异常抛出去只是噪音**,改成永不抛返 bool）、**失败完全不可观测**（用户拿到"已发送"然后永远收不到信,我们一个计数器都不涨——**最坏的故障是只有用户看得见的故障**）;`email_sends{result}` 四档,timeout 与 error 分开（后者常常只是单个收件地址的问题）。阈值和 LLM 反着调：样本门槛更低（3,发码低频,**门槛比流量还高的熔断器等于没有**）、失败比例更高（0.8,不然几个坏地址就带跳闸）。**统计任务刻意不加熔断**：① **熔断的前提是有个可接受的兜底**,而胜场不算就是永久丢账——**没有兜底的依赖,短路它等于直接丢数据**;② 没人在等,"不白等"的收益不存在;③ 已有正确机制（重试 + 幂等）。**熔断适合"失败了就放弃这一次",重试适合"这一次不能丢"——判据不是依赖多重要,是丢掉这次可不可接受**;这条"刻意不做"也钉成测试（源码断言无 breaker 调用）。但**固定 `countdown=5` 是错的**：MySQL 挂 30 秒则 3 次重试全落在同一故障窗口里、全部失败进死信,**重试次数被固定间隔浪费在同一个故障上**;改指数退避 5s→20s→80s（上界 300s,不然涨几轮就是几小时后重试、那时数据早没人关心）。**退避必须带抖动**：一批对局同时结束会被投递成一批任务,重试时刻完全对齐、成排打在刚恢复的数据库上——**重试自己变成下一次故障的原因**（同 F-5：治的是"整齐"不是"量大"）。16 项单测（DEVLOG 041）
- [ ] **S4 复测：10 倍突发下核心对局链路零中断**（唯一必测项,对照组是基线 S4 的 10.09% 失败率）
  - [x] **run A（读接口,50→500→50 阶梯）跑完并修掉它撞出来的一个真 bug：Redis 连接池把过载翻译成 500**。115,344 请求里 99,713 报"失败"(86.45%),拆开看是 **98,159 次 429（我自己的限流器在正常工作）+ 1,376 次 500（真 bug）+ 178 次连接层抖动**——**500 只占失败的 1.4%**。**这个 86.45% 不能和基线的 10.09% 直接比**：基线那次是**超时**（当时无任何限流,请求在 DB/线程池里排队到死),这次是**主动拒绝**（带着 `Retry-After`、后端资源没碰）——**两者在 Locust 眼里都叫 failure,但一个是"系统被打穿了"另一个是"系统拒绝被打穿"**。500 的根因：`core/redis.py` 建池时**什么都没写**、取了 redis-py 默认值 **100**——**没人选过这个数**,而准入层允许突发到 **400**;**保护层的上限宽于它保护的最窄资源,这层保护在这个维度上就没生效**（"门口放 400 人进场、场内 100 把椅子"）,且 redis-py **抛错不是等**,`MaxConnectionsError` 一路冒成 500。**最要紧的不是数字小,是错误的类别**：前三道闸都在如实回答"现在别来",第四道在说"我崩了"——而 **500 在曲线上和代码 bug 长得一模一样**,配置问题会被当成代码问题查。修法：`REDIS_MAX_CONNECTIONS=400`（测试钉的是 **>= `RATE_LIMIT_GLOBAL_CAPACITY`** 这个关系,以后调准入阈值会跟着报错提醒,写死数字就不会）+ 换 `BlockingConnectionPool` 排队 3s + `ConnectionError` 统一兜 **503 + Retry-After** + `redis_pool_exhausted` 指标。**排队 vs 立刻拒的判据是持有时间**：连接借出即还（亚毫秒往返)所以排队必然很快前进,这和 H-3c·上「房间队列刻意不 `await put`」**恰好相反**而判据是同一句话（房间动作要跑十几秒）;但**没有上界的等待只是把排队藏到看不见的地方**,所以配超时。同步池也补显式上限——不写是**另一种失控**：不报错,改为把连接数顶到 Redis 的 `maxclients` 上,**压垮的不是本进程而是所有人共用的那个 Redis**。9 项单测（DEVLOG 042）
  - [x] **对局链路变体 `bench/locust_s4_game.py`：先问"这个场景测的是不是简历要证的那件事"**。`locust_s4.py` 复用 `S1ApiUser`（榜单/最近对局/个人历史,**三个都是读接口**),而简历⑦ 写的是「核心**对局链路**」——建局和提交动作压根不在场景里,**就算全绿,能证明的也只是"读接口在突发下不中断"**;这比不达标更危险,**不达标会逼人去查,而"绿了但测的不是那件事"会被当成已经验证过**。`S4GameUser(S2GameUser)` 只改两处 + `BurstGameShape` 沿用同一形状（50→500→50）以便对照。**刻意继承而不是改 `locust_s2.py`**：S2 测的是稳态吞吐下的动作 P99（简历③ 的数字出处),机器人节奏在那个目的下是对的——**两个场景要两种节奏,统一成一个就必有一个测错**。改的两处：① **一人一账号**（`itertools.count` 按序发号,不用 `random.choice`）——50 个账号跑 500 用户等于每号压 10 个驱动器,而限流键是 `user:{sub}`、动作配额 1 次/秒,**测到的是"同一个账号被 10 倍手速刷",那是限流器在正确工作不是容量边界**;**十倍流量的正确形状是"来了十倍的人",不是"同一批人快十倍"**,账号不够时 `on_start` **直接拒跑不退化成复用**（**一个不说明任何事的红色结果比报错更糟,因为它看起来像个结论**）;② **`wait_time` 降到 1~2s 的真人节奏**,而**刻意不去调高业务配额**——阈值是按"真人能多快"定的,为压测放大它们等于**测了一个线上不存在的系统**,**能改测量方法就别改被测对象**。跑之前还要只放宽 `RATE_LIMIT_IP_*`（500 用户全从 `127.0.0.1` 出去、共享一个 `capacity=60/rate=20` 的桶,稳态只放 20 rps),**而全局层一个字不能动——那正是 S4 要测的那层**。`tick()` 分段逐点验过,**还没跑**。（DEVLOG 043）
  - [x] **核准 run A 数字并改掉两处已提交的错值**（第一铁律）：真值 115,344 / 99,713(86.45%),先前那组是**跑到中途的 `stats.csv` 快照**——**Locust 的 CSV 在跑的过程中就存在且一直被覆写,文件存在不等于测完了**;顺手删掉 `redis.py` 里"387 RPS"那个没出处的数。核准时撞出一个**吞吐悖论**：峰值总 RPS **1,641/s 出现在 100 用户档**（其中 1,501/s 是失败),而峰值**有效** RPS 只有 **278/s、在 500 用户档**——**429 是极廉价的响应**（不查库不碰 Redis不进业务代码),限流器一咬住**总吞吐往上跳而有效吞吐在往下掉**,**只看 RPS 曲线的人会把限流生效读成性能提升**。所以报告里"吞吐"必须写"成功吞吐",同 H-2 那条纪律。
  - [x] **run B（对局链路,环境干净）跑完：042 的修复守住了,但驱动器自己在放大流量,还是没测到对局链路**。环境这次对了：杀掉两天前的旧进程用当前代码重启（`/internal/degrade` 返 **403 不是 404**——**403 才是"跑的是当前代码"的证据**,路由在、只是缺密钥),单节点环,`AI_USE_LLM=False`,只放宽 IP 层,账号 500。**042 验收通过：`redis_pool_exhausted=0`,日志 0 个 500、0 个 503**,上轮那 1,376 次一次没再现。但 38,481 请求 / 28,725 失败(74.65%) 全是 429,**动作只跑了 221 次**。归属对得上账：`admission{global}` 17,922 + `create_game` 10,607 + `game_state` 250 + `action` 1。**根因是驱动器**：`S2GameUser._step` 快照非 200 就 `game_id=None`——**它把 429 理解成"这局没了"**,下个 tick 去建新局;建局也被拒,`game_id` 还是 `None`,**再建一次**。500 用户 120 秒发出 28,729 次建局(成功 3,232)。**这正是 H-3a「429 必须带 Retry-After」那条规则反过来长在客户端身上**——这次服务端说了等多久,**是客户端没听**;**门口保安说"稍等两分钟",这人立刻从队尾重排一遍还顺手又领了张号**。**压测客户端也是个客户端,它对限流的反应方式直接改变你测到的负载形状**;上一轮我改对了"多久发一次",没改"被拒之后做什么"——**这是两个独立的东西**。（DEVLOG 044）
  - [x] **修 run B 撞出的真问题：AI worker 回调自己的 API 会和用户流量抢同一个准入配额**（**这条和压测无关,线上一样会犯,优先级高于继续跑 S4**）。`app/tasks/ai.py:107` 用 `requests.post` 打 `localhost:8000/api/v1/games/{id}/ai_action`,而这条路径**不在 `EXEMPT_PATHS`**（只有 `/health`、`/metrics`、`/`、`/cluster`）。run B 里被拒 **54 次**（27 `ai_action` + 27 `ai_thinking`),celery 日志 54 条 `API Error 429` 一一对应。回调被拒 → AI 任务抛异常 → 退避重试 → **房间推进不了** → 玩家以为这局没了去建新局 → **过载自己长出一条正反馈**。**判据和 `/health` 豁免是同一条**：拒健康检查会让 LB 摘节点、剩下的更快被拒（限流器亲手把过载放大成雪崩),拒 AI 回调会让房间卡住、玩家重建局,**形状一模一样**。**凡是"系统为了自愈而发给自己的请求"都不该和外部流量抢配额**——它被拒的代价不是少服务一个人,而是**已服务到一半的那些人全部卡住**。修法二选一：豁免带内部密钥的内部回调,或给它独立配额。**已修：`admission.py` 加 `is_internal_request()`,中间件里挂在 `EXEMPT_PATHS` 之后、跨节点转发豁免之前（**这不是"少算一次",而是"根本不该被算"**）。四个决定：① **按凭据不按路径**——回调路径带 `{game_id}` 而 `EXEMPT_PATHS` 是精确匹配的 frozenset,改成前缀匹配 `/api/v1/games` 等于**把整个对局接口从准入里摘出去**,而那正是最需要保护的地方;**豁免的判据只能是"证明得了自己是内部请求",不能是"长得像内部请求"**;② **必须验密钥不能只看头在不在**,否则加个请求头就能绕过整层准入——**一个可伪造的凭据等于没有凭据**（`X-Forwarded-For` 那个错误再犯一次）;③ **`hmac.compare_digest` 不用 `==`**,这个比较发生在**每一个请求**上,朴素比较在第一个不同的字节上短路,**能被计时的比较就是能被问出来的秘密**（**你说"密码错了"的速度本身就在告诉别人错在第几位**);路由里那几处 `!=` 同样该改但刻意不混进来;④ 顺带覆盖 `/internal/*`——**过载正是最需要拧降级开关的时刻**,那时把操作入口也拒了等于事故中锁死方向盘。4 项新测试,两条压舱（错密钥/无头/空串都不放行;豁免名单里不出现 `ai_action`/`ai_thinking`,把"别改成按路径"钉在测试里),时序那条用 `inspect.getsource` 断言源码含 `compare_digest`——**行为上测不出时序差异,能测的只有"选没选对那个函数"**。（DEVLOG 045）
  - [x] **顺手撞出并修掉一个真 bug：`game_service.py` 用了 `degrade` 但从没 import 它,回放事件流每次都 500**。`NameError: name 'degrade' is not defined`（`:806`,`get_game_events_cached` 里 `await degrade.cold_path_interval(...)`）。`git log -S` 定位到引入者是 `d01e928`（H-4 五级降级矩阵),**和准入改动无关**。**根因不是少写一行 import,是这条读路径没有任何测试**（`grep -rl get_game_events_cached backend/tests/` 零命中)——于是 **L3 那段降级代码从写下来到今天一次都没被执行过**,`NameError` 只在运行到那行时才抛,不跑就不报。它恰好是 F 组两级缓存和 H-4 降级矩阵的**交点**：**两个组件各自都测过,交点没人测**;**两条路各自验收合格,但没人走过那个路口**。**"测试全绿"不等于"代码都跑过"**——覆盖率工具看得出这件事,读代码看不出来。修法一行 import。（DEVLOG 045）
  - [ ] **改驱动器：区分"这次没成功"和"这局结束了",并对 429 照着 `Retry-After` 退避**。现在一切非 200 都被翻译成"重新建局"——**同一个状态位承载两种含义,压力下必然选错那种**。
  - [ ] **环境前提（run A 踩过）**：8000 端口那套服务是两天前起的进程,`/internal/degrade`、`/internal/breakers` 全 404——**拿它跑 S4 测的是 H 组之前的系统**,压测前必须 curl 一个新加的路由验一次。而另起实例会**通过共享 Redis 加入同一个一致性哈希环**（房间被哈希到两个节点、跨节点转发失败,run A 时集成测试因此报 502）,**压测实例不是旁观者**,跑完必须停掉。

---

## 已砍清单（简历不提,一律不做）

Go 接入层 · Design Only 文档（分库分表/单元化/多活）· 结构化 JSON 日志与脱敏 · 并发用例（Actor 串行化已覆盖）· 缓存预热与就绪探针 · 热榜赛季机制 · ADR 4 篇（经验已在 DEVLOG,不再单独成文）· README 架构演进叙事 · 额外压测报告归档 · RuleEngine/LLMEngine 接口抽象（两条路径已能跑,抽类只是重构）· S2 动作 TPS 5,000 目标 · S3 冲 20,000 连接 · S3/S5 拆分后复测 · 网关节点宕机连接迁移演练 · 各类 ADR 与演练留档

## 顺序

```
E（网关 + 广播）→ F（缓存）→ G（热榜）→ H（韧性,含 AI 开关前置）→ 跑一次 S4 结清简历⑦
```
