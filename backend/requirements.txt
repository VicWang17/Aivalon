fastapi>=0.128.0,<0.129.0  # 锁定基线实测版本；0.141 引入 _IncludedRouter，与 instrumentator 7.x 的 route.path 假设不兼容
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.25
alembic>=1.13.1
aiomysql>=0.2.0
pymysql>=1.1.0
redis>=5.0.1
celery>=5.3.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
pydantic-settings>=2.1.0
python-multipart>=0.0.9
requests>=2.31.0
cryptography>=42.0.0
# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0  # room_actor/幂等测试是 async 用例；缺失时 pytest 9 直接判 fail（旧机为手动补装，此处补上显式依赖）
httpx>=0.26.0
openai>=1.0.0
fastapi-limiter==0.1.6
fastapi-mail>=1.4.1
# Observability
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=7.0.0,<8.0.0  # 8.x 强制 starlette 1.x，与 fastapi 0.128 的 starlette<0.51 冲突
# Bench
locust>=2.30.0
