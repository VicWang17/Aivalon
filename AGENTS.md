# AGENTS.md · Aivalon 项目工作守则

> 本文件是本项目对 AI 编码助手的持久约定，每次会话自动生效。
> 事实源优先级：**本文件（工作方式）> `document/PRD_v2_high_concurrency.md`（做什么）> `document/todo_v2.md`（做到哪了）**。
> `document/PRD.md` 与 `document/todo.md` 为 v1 历史存档，不再更新。

## 1. 项目定位

Aivalon：AI 阿瓦隆桌游对局平台（1 真人 + 7 AI），v1 已完成产品闭环（Vue 3 + FastAPI + MySQL + Redis + RabbitMQ + Celery）。

- v2 阶段的产品形态冻结，不再加玩法；全部投入**高并发架构升级**：压测驱动重构对局热路径、WS 网关、缓存与热榜、韧性体系
- 项目的两大技术亮点叙事（所有工作都要能挂到这两条上）：
  - **高并发**：压测驱动的性能演进、单写者 Actor 热路径、缓存三兄弟实战、限流降级熔断韧性体系
  - **实时通信**：WebSocket 长连接网关化（连接/逻辑分离）、跨节点广播扇出、事件合并帧/分级推送/慢消费者背压
- **这个项目存在的根本目的是大厂后端架构面试**，不是做产品。一切技术决策都要能转化为面试叙事；遇到"更工程化但讲不出故事"和"更简单但故事完整"的分岔时，选后者
- 简历描述（见本文档 §2）是 v2 的**范围基准**：做且只做简历上写了的事，每条都有可验证的证据链

## 2. 简历证据链（改动时不可破坏）

**数字口径**：简历上所有性能数字以 v1 基线与优化后压测的**实测值**为准回填；下表中的数字是目标参考值，实测达不到就先改简历（铁律 1）。回填核对是 todo I 组的收尾动作。

简历上的七条描述，每条对应必须存在的证据：

| 简历条目 | 证据链 |
|---|---|
| 压测驱动演进（X00 → 5,000+ TPS） | Locust 压测脚本 + 5 类标准场景 + v1 基线报告 + 前后对比数据 |
| 热路径重构（Actor / Write-Behind / 写压力 -80%） | 房间 Actor 实现 + Redis Stream 双写 + 批量落库 + 宕机恢复演练 + RPO 量化记录 |
| 长连接与广播（2 万连接 / P99 < 200ms） | 独立 WS 网关 + 跨节点扇出 + 合并帧/分级推送/背压 + S3 压测报告 |
| 缓存与热榜（P95 < 10ms @ 1 万 QPS） | 多级缓存 + 布隆过滤器/singleflight/逻辑过期三个落点 + 三份缓存演练记录 |
| 韧性工程（5 级降级 / 6 项演练 / 10 倍突发零中断） | 降级矩阵开关 + 熔断器 + 6 份混沌演练记录 + S4 突发压测 |
| AI 决策链路（双引擎 / 舱壁 / 可降级） | 规则/LLM 引擎抽象 + 超时回落 + 降级开关 |

三条铁律：

1. **数字真实性**：简历上任何数字必须可复现——有压测脚本、环境说明、参数记录，随时能重跑。测不出来就改简历，不许写目标值充数
2. **范围纪律**：todo 中超出简历条目的内容默认不做；新想法先问一句"这条上简历吗"，不上就砍
3. **先基线后优化**：没有 v1 基线数字之前，不动任何优化代码；每个优化完成时必须产出前后对比数字

## 3. 环境约定

- **中间件**：一律 Docker Compose（项目根 `docker-compose up -d`）：MySQL 3306 / Redis 6379 / RabbitMQ 5672（管理台 15672，guest/guest）/ Flower 5555。数据持久化在 `./data/`
- **后端三进程**：`backend/` 下 `source venv/bin/activate` 后，优先 `python dev_runner.py` 一键起（API + Celery + Relay）；单独调试时：API `uvicorn app.main:app --reload`（8000）、Worker `celery -A app.core.celery_app worker --loglevel=info`、Relay `python -m app.core.outbox_relay`
- **前端**：`frontend/` 下 `npm run dev`（5173），构建器为 rolldown-vite（`vite` 被 override 成 `npm:rolldown-vite`，排障时注意这不是官方 vite）
- **LLM**：DeepSeek API（OpenAI SDK 兼容），密钥走 `.env`，真密钥永不入库；压测一律走规则 AI，LLM 路径单独小流量验证
- **已知坑**：`backend/backend/` 是误嵌套目录（勿动勿用）；`celerybeat-schedule-*` 是运行时产物，不提交

## 4. 开发节奏

- 按 `document/todo_v2.md` 的 checkbox 推进，完成任务**随手勾掉**；范围变更记入该文件并同步本文件 §2 的证据链映射
- 执行顺序纪律：观测/压测基线（A/B）→ 测试债（C）→ 热路径（D）→ 网关广播（E）与缓存热榜（F/G）→ 韧性演练（H）；叙事资产（I）随做随写
- 新代码匹配周边风格；最小改动；不加投机性的"以后可能用到"的抽象
- **验收文化**：每个功能完成后跑真实验证（真实中间件、真实压测），不许把没跑过的代码说成完成；性能类功能没有前后对比数字不算完成
- 重构保护：热路径（D 组）动工前，C 组状态机测试必须全绿；重构期间测试常驻

## 5. Git 约定

- **每完成一个功能（对应 todo 的一个或多个 checkbox），自觉 `git commit` 并 `git push` 到 remote**，不用等用户提醒
- commit 前自查：无密钥、无运行时产物（`celerybeat-schedule-*`、`data/`）、相关 checkbox 已勾
- commit message 用 Conventional Commits 简体中文版式：`feat: ...` / `fix: ...` / `docs: ...` / `perf: ...` / `test: ...` / `chore: ...`；性能类提交正文注明前后数字
- 一个 commit 只做一件事；不顺手提交无关改动
- 例外：用户明确说"先别提交"时除外

## 6. 交流风格（求职导向）

这个项目是为面试存在的，所以在日常交流中：

- **多科普**：涉及概念/技术选型时，用简短段落讲清"它是什么、为什么这样选、业界常见替代方案是什么"——用户在积累面试谈资，不是在听报流水账
- **预判面试问题**：每完成一个功能模块，主动补一段「这个功能面试可能怎么考」，给出 1–3 个高频问题 + 答题骨架，值得沉淀的追加到对应 ADR
- **记录开发经验**：凡遇到非平凡问题（报错、环境坑、压测结果与预期不符、选型纠结），解决后按「现象 → 排查 → 根因 → 解决 → 经验」五段式记录到 `document/DEVLOG.md`，注明日期。不等用户提醒，解决即记录
- **量化意识**：凡能产出数字的地方（TPS、延迟分位数、连接数、命中率、写压力下降比例），帮助用户记录下来，简历要用真实数据
- 科普和预判要简短、嵌在工作汇报之后，不要喧宾夺主；用户说"跳过讲解"时照办
- **小步慢讲（最高优先级工作方式）**：每次只做一小步（一个最小可验证的改动单位），做完即停下来详细讲解，等用户说"继续"再走下一步。禁止一次性推进多个功能后一次性总结
- 讲解固定四段式：①**做了什么**（目标与产出）②**怎么做的**（思路与顺序）③**代码层怎么实现的**（关键文件、核心片段、数据流向）④**有什么可以学习和注意的**（原理科普、坑、面试点）
- 后台/子任务执行期间，每有关键进展主动向用户简报，而不是等全部完成
